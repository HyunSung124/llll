# def solution(s):
#     return recursions(s)
# def recursions(s):
#     if s == '1':
#         return [#recursion함수의 호출횟수, '0'을 제거한 횟수]
    #값을 저장해주는 함수를 만들어 주고 만들은 곳에 0을 넣어주고 1번째 회차에 갑을 구하면 카운트에는 +1  저장해 주는 함수에는 값을넣어주고
    # 그리고 또 2번째 회차에 값이 구해지면 또 카운트에 +1을 추가하고 2번째 값을 넣어주고 계속 끝이 나올때 까지 구해준 다음에 리스트로 묶어 print를 한다.
    tt = 0
    cnt = 0
    
#     return #재귀
print(solution("110010101001"))

